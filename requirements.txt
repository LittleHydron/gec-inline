# Runtime dependencies — what the Gradio app / HF Space needs.
# Eval + test extras (ERRANT, spacy, pytest) live in requirements-dev.txt.
torch>=2.2.0
transformers>=4.45.0
peft>=0.13.0
accelerate>=0.34.0
gradio>=4.44.0
spaces>=0.30.0
huggingface_hub>=0.24.0
numpy>=1.26
tqdm>=4.66
