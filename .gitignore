.venv/
__pycache__/
*.pyc
.pytest_cache/
.ipynb_checkpoints/

# Raw datasets are downloaded on demand and may be license-gated.
data/raw/

# Big binaries
*.safetensors
*.bin
*.pt

# Local secrets
.env

# HF cache / Spaces build artifacts
.gradio/
flagged/

# Per-checkpoint outputs — regenerated by scripts.generate / scripts.eval.
# results/qualitative.md is kept (small, hand-curated reference table).
results/predictions/
results/metrics/
